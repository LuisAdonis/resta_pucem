import 'package:flutter/material.dart';

class TerminosyCondicionesView extends StatelessWidget {
  const TerminosyCondicionesView({super.key});
  static const routeName = 'terminosycondiciones';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("---"),
        centerTitle: true,
      ),
    );
  }
}
